#from kubuntu. move.
cd ~/Code/intent/src/robot-intent-robot/&&git pull
cd ~/Code/robot-intent-controller/ && git pull

#from arch. sources rc even if not interactive
#[[ -f ~/.bashrc ]] && . ~/.bashrc