# pkgs/debian.txt — Debian / Ubuntu (apt). One entry per line; blank and # are ignored.
# Entries: PKGNAME[:BINNAME] — BINNAME defaults to PKGNAME. Used to verify the
# install actually produced a usable binary (and to look up the ubi fallback
# project when the system PM can't provide it).

# Modern CLI tools
eza
bat
ripgrep:rg
fd-find:fd
fzf
zoxide
starship
git-delta:delta
lazygit
gh
tealdeer:tldr
navi
fastfetch
atuin
direnv
tmuxinator
tpack

# Base shell, multiplexer, editors
zsh
tmux
vim
nano
git
curl
